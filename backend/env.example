# Variáveis de ambiente para AutoUnitTest Backend

# Ambiente de execução
NODE_ENV=development

# Porta do servidor
PORT=3000

# CORS - origem permitida
CORS_ORIGIN=http://localhost:4200

# Banco de dados (opcional)
# DATABASE_URL=postgresql://user:password@localhost:5432/autounittest

# JWT Secret (opcional)
# JWT_SECRET=your-super-secret-jwt-key

# OpenAI API Configuration
OPENAI_API_KEY=YOUR_OPENAI_API_KEY

# Nível de log
LOG_LEVEL=info

# ===== AI Provider Switch =====
# openai | stackspot
AI_PROVIDER=openai

# ===== Stackspot (apenas se AI_PROVIDER=stackspot) =====
# Obtidos no portal Stackspot (exibidos uma vez)
# IMPORTANTE: mantenha em segredo, não commitar
STACKSPOT_CLIENT_ID=
STACKSPOT_CLIENT_KEY=

# Realm padrão: stackspot-freemium
STACKSPOT_REALM=stackspot-freemium

# Opcional: sobrescrever URL de token (senão monta com o realm)
# STACKSPOT_TOKEN_URL=https://idm.stackspot.com/stackspot-freemium/oidc/oauth/token

# User-Agent exigido pelo Stackspot
STACKSPOT_USER_AGENT=AutoUnitTest/1.0 (+backend)

# Endpoint de completions do Stackspot (compatível com schema OpenAI)
# Ex.: https://genai.stackspot.com/v1/chat/completions
STACKSPOT_COMPLETIONS_URL=

# Endpoint de Agent Chat do Stackspot (se usar agente)
# Ex.: https://genai-inference-app.stackspot.com/v1/agent/<AGENT_ID>/chat
STACKSPOT_AGENT_CHAT_URL=