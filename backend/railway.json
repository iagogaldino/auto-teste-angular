# Railway deployment configuration
# Este arquivo é usado pelo Railway para configurar o deploy

# Comando para instalar dependências
install: npm install

# Comando para build
build: npm run build

# Comando para iniciar o servidor
start: npm start

# Porta será definida automaticamente pelo Railway
# Variáveis de ambiente necessárias:
# NODE_ENV=production
# CORS_ORIGIN=https://seu-dominio.railway.app
